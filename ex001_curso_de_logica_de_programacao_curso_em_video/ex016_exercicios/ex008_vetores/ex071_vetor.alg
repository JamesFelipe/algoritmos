algoritmo "vetor"
var
numeros: vetor[1..7] de inteiro
i: inteiro
inicio


// Preenchendo os valores automaticamente
para i de 1 ate 7 faca
   aleatorio 1, 9
   leia(numeros[i])
fimpara

// Exibindo os valores da matriz
para i de 1 ate 7 faca
  escreva(numeros[i])
fimpara
fimalgoritmo
